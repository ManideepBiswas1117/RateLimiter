version: '3'
services:
  redis:
    image: redis:latest
    container_name: redis
    ports:
      - "6379:6379"
  app:
    build: .
    depends_on:
      - redis
    environment:
      - SPRING_REDIS_HOST=redis # Point to the Redis service name
    ports:
      - "8080:8080"
